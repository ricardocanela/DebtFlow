{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "title": "Request Rate (RPS)",
      "type": "timeseries",
      "datasource": "Prometheus",
      "targets": [
        { "expr": "sum(rate(django_http_requests_total_by_method_total[5m]))", "legendFormat": "Total RPS" }
      ],
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 }
    },
    {
      "title": "Error Rate (%)",
      "type": "timeseries",
      "datasource": "Prometheus",
      "targets": [
        { "expr": "sum(rate(django_http_responses_total_by_status_view_method{status=~\"5..\"}[5m])) / sum(rate(django_http_responses_total_by_status_view_method[5m])) * 100", "legendFormat": "5xx %" }
      ],
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 }
    },
    {
      "title": "P95 Latency (ms)",
      "type": "timeseries",
      "datasource": "Prometheus",
      "targets": [
        { "expr": "histogram_quantile(0.95, sum(rate(django_http_requests_latency_seconds_by_view_method_bucket[5m])) by (le)) * 1000", "legendFormat": "P95" }
      ],
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 }
    },
    {
      "title": "Status Code Distribution",
      "type": "piechart",
      "datasource": "Prometheus",
      "targets": [
        { "expr": "sum by (status) (increase(django_http_responses_total_by_status_view_method[1h]))", "legendFormat": "{{status}}" }
      ],
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 }
    }
  ],
  "refresh": "30s",
  "schemaVersion": 38,
  "tags": ["debtflow", "api"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "title": "DebtFlow — API Overview",
  "uid": "debtflow-api-overview"
}
